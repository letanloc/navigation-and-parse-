<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.loc.coffemanager" >

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".Application.controllerApplication"
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity android:name=".Login" />
        <activity android:name=".Coffee.Coffelist" />
        <activity android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CreateNewUser"
            android:label="@string/title_activity_create_new_user"
            android:theme="@style/AppTheme" />
        <activity android:name=".Coffee.Main2Activity" >
        </activity>
        <activity android:name=".Coffee.numberofproducts" >
        </activity>
        <activity android:name=".changeprodus" >
        </activity>
        <activity android:name=".Cart" >
        </activity>
    </application>

</manifest>
